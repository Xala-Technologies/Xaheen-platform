{
  "name": "@xaheen-ai/monorepo",
  "version": "1.0.0",
  "description": "Xaheen Enterprise Monorepo - CLI, MCP, Design System, and Web Applications",
  "private": true,
  "author": "Xaheen Team",
  "license": "MIT",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "clean": "turbo clean",
    "test": "turbo test",
    "format": "turbo format",
    "check": "turbo check",
    "install:clean": "pnpm clean && pnpm install",
    "update-deps": "pnpm update",
    "typecheck": "turbo typecheck",
    "deploy": "turbo deploy"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.0.0"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=9.0.0"
  }
}