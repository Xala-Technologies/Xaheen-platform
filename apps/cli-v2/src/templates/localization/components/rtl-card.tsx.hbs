{{!-- Template: rtl-card.tsx.hbs --}}
{{!-- Category: localization --}}
{{!-- Type: component --}}
{{!-- Migrated from: /Volumes/Development/Xaheen Enterprise/xaheen/apps/cli/templates/localization/ar/components/rtl-card.tsx.hbs --}}
import React from 'react';
{{#if typescript}}
import type { ReactNode } from 'react';
{{/if}}

{{#if typescript}}
interface RTLCardProps {
  title?: string;
  children: ReactNode;
  isRTL?: boolean;
  className?: string;
}
{{/if}}

export const RTLCard{{#if typescript}}: React.FC<RTLCardProps>{{/if}} = ({
  title,
  children,
  isRTL = true,
  className = '',
}) => {
  return (
    <div
      className=""`}
      dir={isRTL ? 'rtl' : 'ltr'}
    >
      {title && (
        <div className=""`}>
          <h3 className="text-lg font-semibold text-gray-900">{title}</h3>
        </div>
      )}
      <div className="p-6">
        {children}
      </div>
    </div>
  );
};