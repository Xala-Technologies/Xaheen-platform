import React from 'react';
import { 
  Button, 
  Card, 
  CardContent, 
  CardDescription, 
  CardHeader, 
  CardTitle,
  Container,
  Stack,
  Text,
  Grid
} from '@xaheen/design-system';
import { t } from '@xaheen/design-system/i18n';
{{#if_contains features 'dashboard'}}
import { Dashboard } from '@xaheen/design-system';
{{/if_contains}}
{{#if_contains features 'navbar'}}
import { Navbar } from '@xaheen/design-system';
{{/if_contains}}

export default function {{pascalCase name}}Page(): JSX.Element {
  // v5.0 Architecture: Perfect semantic structure with Stack components
  // AI-Optimized: Uses semantic naming and predictable patterns
  // Norwegian Compliance: Supports i18n with nb-NO default

  return (
    <Container size="xl">
      <Stack direction="vertical" gap="xl">
        {{#if_contains features 'navbar'}}
        <Navbar variant="flat" />
        {{/if_contains}}
        
        {/* Hero Section */}
        <Stack direction="vertical" gap="lg" align="center">
          <Text variant="h1" align="center">
            {t('welcome.title', { name: '{{title}}' })}
          </Text>
          <Text variant="body" align="center" color="secondary">
            {t('welcome.description', { description: '{{description}}' })}
          </Text>
        </Stack>

        {{#if_contains features 'dashboard'}}
        {/* Dashboard Layout Pattern */}
        <Dashboard 
          variant="modern"
          spacing="xl"
        />
        {{else}}
        {/* Feature Grid Pattern */}
        <Grid cols={{ base: 1, md: 2, lg: 3 }} gap="lg">
          <Card>
            <CardHeader>
              <CardTitle>{t('actions.getStarted.title')}</CardTitle>
              <CardDescription>
                {t('actions.getStarted.description')}
              </CardDescription>
            </CardHeader>
            <CardContent>
              <Button variant="primary" size="lg" fullWidth>
                {t('actions.getStarted.button')}
              </Button>
            </CardContent>
          </Card>

          <Card>
            <CardHeader>
              <CardTitle>{t('actions.documentation.title')}</CardTitle>
              <CardDescription>
                {t('actions.documentation.description')}
              </CardDescription>
            </CardHeader>
            <CardContent>
              <Button variant="outline" size="lg" fullWidth>
                {t('actions.documentation.button')}
              </Button>
            </CardContent>
          </Card>

          <Card>
            <CardHeader>
              <CardTitle>{t('actions.examples.title')}</CardTitle>
              <CardDescription>
                {t('actions.examples.description')}
              </CardDescription>
            </CardHeader>
            <CardContent>
              <Button variant="ghost" size="lg" fullWidth>
                {t('actions.examples.button')}
              </Button>
            </CardContent>
          </Card>
        </Grid>
        {{/if_contains}}
      </Stack>
    </Container>
  );
}