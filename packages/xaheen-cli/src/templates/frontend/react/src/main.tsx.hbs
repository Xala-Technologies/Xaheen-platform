{{!-- Template: main.tsx.hbs --}}
{{!-- Framework: react --}}
{{!-- Type: entry --}}
import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';
import './index.css';

ReactDOM.createRoot(document.getElementById('root'){{#if typescript}} as HTMLElement{{/if}}).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);